# --- Docker Compose 配置 ---

# 宿主机端口，对应脚本中的 HOST_PORT
HOST_PORT=7862

# 代理 URL，对应脚本中的 PROXY_URL
# 如果不需要代理，请留空或注释掉此行
PROXY_URL="http:192.168.31.198:12156"

# 注意：app.env 文件中的应用专用密钥等，仍然由 Docker Compose 的 env_file 指令加载。
# 这个 .env 文件主要用于控制 Docker Compose 本身的行为。
